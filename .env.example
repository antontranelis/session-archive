# Session-Archiv Konfiguration
# Kopiere diese Datei nach .env und passe sie an:
#   cp .env.example .env

# === Pflicht ===

# Pfad zu deinen JSONL-Session-Dateien
# Claude Code speichert Sessions als JSONL unter ~/.claude/projects/
SESSIONS_DIR=/home/deinname/.claude/projects

# User-Mapping: wer:pfad (im Container)
# Für einen User:
USERS=meinname:/app/sessions
# Für mehrere User (Unterordner im SESSIONS_DIR):
# USERS=anton:/app/sessions/anton timo:/app/sessions/timo

# === Optional ===

# Anthropic API Key für automatische Zusammenfassungen + Tags
# Ohne Key funktioniert alles, aber ohne Summaries/Tags/Graph-Destillation
# ANTHROPIC_API_KEY=sk-ant-...

# Port (default: 8111)
# PORT=8111

# API Key für Remote-Zugriff (leer = offener Zugang, gut für lokal)
# ARCHIVE_API_KEY=

# URL-Prefix wenn hinter Reverse-Proxy (z.B. /archive)
# BASE_PATH=
